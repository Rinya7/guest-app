# Guest PWA - Завдання та Прогрес

## 📋 ОПИС ПРОЕКТУ (Якщо б він був готовий)

### Призначення
Progressive Web App (PWA) для гостей готелів. Дозволяє гостям отримувати доступ до інформації про своє проживання через унікальний токен доступу, без необхідності реєстрації або авторизації.

### Основні можливості

#### 1. Доступ по токену
- Унікальний лінк для кожного проживання
- Безпечний доступ без реєстрації
- Автоматичне визначення рівня доступу на основі статусу Stay

#### 2. Різні екрани залежно від статусу Stay

**Статус `booked` (бронь створена, ще не заїхав):**
- Назва готелю, логотип
- Номер кімнати
- Дати check-in, check-out
- Кількість гостей
- Статус "Бронювання підтверджено"
- Правила заїзду/виїзду (години policy)
- Контакти готелю (телефон, email, адреса, карта)
- Можливість додати коментар/побажання
- Інструкції (паркування, як знайти готель)

**Статус `occupied` (гость заїхав, живе):**
- Усе зі статусу `booked`
- Wi-Fi дані (назва мережі та пароль)
- Інформація про номер
- Кнопка "Попросити прибирання"
- Кнопка "Написати в ресепцію" (WhatsApp/телефон)
- Інформація про останнє прибирання

**Статус `completed` (виїхав):**
- Подяка за проживання
- Дати проживання
- Готель, номер
- Короткий фінансовий підсумок (якщо є)
- Кнопка "Залишити відгук"
- Кнопка "Забронювати ще раз"

**Статус `cancelled` (скасовано):**
- Інформація про скасування
- Контакт для зв'язку

#### 3. PWA функції
- Можливість встановити на домашній екран
- Офлайн доступ до базової інформації
- Push-нотифікації (в майбутньому)
- Швидкий доступ до Wi-Fi паролю

#### 4. Інтеграція з Backend
- Отримання даних через Guest API
- Автоматичне оновлення статусу
- Відправка запитів (прибирання, відгуки)

### Технологічний стек
- **Framework:** Vue 3 (Composition API)
- **Routing:** Vue Router
- **HTTP Client:** Axios або Fetch
- **Styling:** Tailwind CSS (або інший)
- **PWA:** Service Worker, Web App Manifest
- **TypeScript:** Строга типізація
- **Build Tool:** Vite

### Архітектура
- **Single Page Application** з динамічними маршрутами
- **Token-based** доступ без авторизації
- **Responsive design** для мобільних пристроїв
- **Offline-first** підхід для базової інформації

---

## 📊 ХІД РОБОТИ

### 0.  План етапів (щоб ми не загубились)

    Я пропоную такий порядок для гостьової частини:

    1. Прописуємо логіку й ролі
       - Як гість отримує доступ
       - Які екрани/функції в різних статусах: booked, occupied, completed
    2. Проєктуємо бекенд для гостя
       - Нова сутність типу GuestAccessToken
       - Ендпоінти /guest/...
       - Які дані віддаємо, які — ні
    3. Проєктуємо фронт PWA для гостя (Vue)
       - Структура сторінок, роутів
       - Що показуємо на кожному етапі
    4. Реалізація бекенда (код + коменти)
    5. Реалізація фронта (код + коменти)

    Зараз робимо етап 1: логіка, рівні доступу, зв’язок із тим, що у тебе вже є.
====================================================================================
1.  Як гість взагалі потрапляє в систему?
У тебе вже є сутність Stay і вся логіка бронювань. Це наш центр світу.

    🧩 Два основні сценарії (як ти і описав):

    1. Гість сам бронює через майбутній сайт-вітрину
        - Вибрав готель → вибрав кімнату → забронював
        - Після успіху:
            - створюється Stay у твоєму бекенді
            - система генерує гостьовий токен
            - гість отримує лінк виду:
                - https://guest.yourdomain.com/s/{token}
        - ЦЕ ми реалізуємо пізніше, коли будемо робити booking engine.
    2. Бронювання створив редактор / адміністратор
        - В адмінці створили Stay
        - В інтерфейсі admin-frontend кнопка:
            "🔗 Згенерувати лінк для гостя"
        - Виклик до бекенду → створюється або повертається існуючий GuestAccessToken
        - Адміністратор:
            - копіює лінк та скидає в WhatsApp/email/SMS
        - Гість відкриває → потрапляє в гостьовий кабінет.
Висновок:
🔑 Все крутиться навколо Stay + окремого токена доступу (один або кілька на стей).
======================================================================================
2. Рівні доступу в залежності від статусу Stay
У тебе вже є статуси booked | occupied | completed | cancelled.
Ми не міняємо їх, а просто прив’язуємо можливості гостя до кожного.

🟡 Етап 1: Статус booked (бронь створена, ще не заїхав)

Що гість може бачити/робити:

Бачити:
    - Назва готелю, логотип
    - Номер кімнати (або тип номера, якщо так вирішимо)
    - Дати: check-in, check-out
    - Кількість гостей
    - Статус: “Бронювання підтверджено”
    - Правила заїзду/виїзду (години policy)
    - Контакти готелю (телефон, email, адреса, карта)
    - Можливо: орієнтовна сума / баланс (якщо ти хочеш це показувати)

Робити (мінімально):
    - Додати коментар / побажання (наприклад, “пізній заїзд”, “алергія”)
    - Прочитати інструкції (паркування, як знайти готель і т.п.)

Поки НЕ показуємо:
    - Wi-Fi пароль
    - Інформацію по прибиранню
    - Будь-які внутрішні технічні статуси
===========================================================================
===========================================================================
🔵 Етап 2: Статус occupied (гость заїхав, живе)
Тут ми відкриваємо більший доступ, як ти і хотів.

Бачити:
    - Усе, що було в booked
    - Інформацію про номер:
        - Номер кімнати
        - Поточний статус кімнати (occupied, cleaning, free — але в UX ми це красиво назвемо)
        - Чи була остання уборка (можемо потім підтягнути з RoomCleaningLog, у тебе така сутність уже є)
    - Wi-Fi дані:
        - Якщо у кімнати є wifiName, wifiPassword → показуємо їх
        - Якщо в Admin є defaultWifiName/defaultWifiPassword → fallback
    - Інфо про додаткових гостей (з extraGuestNames)

Робити:
    - Кнопка “Попросити прибирання” / “Room cleaning request”
    - Кнопка “Не турбувати” (якщо захочеш в майбутньому)
    - Кнопка “Написати в ресепцію” (на старті можна просто відкривати WhatsApp/телефон, без чату)
    - “Продовжити проживання” (в майбутньому – запит на продовження дат)
============================================================================
============================================================================
⚪ Етап 3: Статус completed (виїхав)

Бачити:
    - Подяка за проживання
    - Дати проживання
    - Готель, номер
    - Можливо: короткий фінансовий підсумок (якщо будемо це зберігати)
    - Кнопка “Залишити відгук”
    - Кнопка “Забронювати ще раз” (в майбутньому — лінк на booking-сторінку)

Що з доступом далі:
    - Можемо:
        - або залишити лінк робочим (тільки read-only info)
        - або через Х днів токен протухає (expiresAt)
============================================================================
============================================================================
🔴 Етап 4: Статус cancelled

Тут все просто:

Показуємо:
    - що бронювання скасовано
    - контакт для зв’язку, якщо це помилка
Ніяких функцій, тільки інфо.

============================================================================
============================================================================
3. Що нам потрібно додати в бекенд логічно (без коду, тільки концепція)

Щоб це все працювало, нам потрібно:

🧱 Нова сутність: умовно GuestAccessToken

Ідея така:
    - Один Stay → 0..N гостьових токенів
    - Токен = довгий випадковий рядок (типу HMAC-SHA256 / uuid + додаткове шифрування)

Приблизний склад (концептуально):
        // Це ще не реальний entity-клас TypeORM, а логічний опис
                interface GuestAccessToken {
                    id: number;
                    stayId: number; // зв'язок із Stay
                    token: string; // випадковий секретний токен
                    createdAt: Date;
                    expiresAt: Date | null; // можемо робити безстроковим, або +1 рік
                    revokedAt: Date | null; // якщо адмін захоче “відкликати” доступ
                    lastUsedAt: Date | null; // для статистики
                }
        в реальному коді зробимо нормально, з TypeORM entity, без any, з коментарями.

============================================================================
============================================================================
🌐 Нові ендпоінти для гостя (чернетка)

    1. POST /guest/stays/:id/token
        - Хто викликає: admin/editor з адмінки
        - Що робить:
            - генерує новий токен для stay (або повертає існуючий активний — залежить від логіки, яку ми придумаємо)
            - повертає готовий URL для відправки гостю

    2. GET /guest/access/:token
        - Хто викликає: браузер гостя (без логіну)
        - Що робить:
            - перевіряє, чи токен валідний, не прострочений, не відкликаний
            - знаходить Stay + пов’язаний Room + Admin (hotel)
            - на основі статусу stay формує view-об’єкт з полями, які можна показувати гостю
            - повертає 401/403, якщо токен не валідний
    3. (Пізніше) POST /guest/access/:token/requests/cleaning
        - відправляє запит на прибирання (ми його потім покажемо в адмінці)
    4. (Пізніше) ще /feedback, /extend, тощо.
============================================================================
============================================================================
4. Що НЕ будемо показувати гостю з бекенда

Щоб не палити внутрішню кухню, гість не повинен бачити:
    - логін/юзера, хто створив бронювання (editor/admin)
    - будь-які внутрішні ID (AdminId, RoomId тощо) — максимум roomNumber як текст
    - історію логів у деталях (Audit Center тільки для адмінки)
    - дані про інших гостей/номери/броні
    - фінансові деталі, які не мають бути публічними (наприклад, внутрішні тарифи, собівартість)
Тому ми робитимемо окремий “чистий” DTO для гостя, щось типу:
    interface GuestStayView {
        stayId: number;
        hotelName: string;
        hotelAddress: string | null;
        roomNumber: string;
        status: "booked" | "occupied" | "completed" | "cancelled";
        checkIn: string;   // YYYY-MM-DD
        checkOut: string;  // YYYY-MM-DD
        mainGuestName: string;
        extraGuestNames: string[];
        wifiName?: string;
        wifiPassword?: string;
        policyCheckInHour: number | null;
        policyCheckOutHour: number | null;
        contactPhone?: string;
        contactEmail?: string;
    }
    І саме його віддаватиме GET /guest/access/:token.

============================================================================
============================================================================
5. Як це ляже на майбутнє “каталог всіх готелів”
    Ти хотів мати: сайт, де всі твої готелі, людина вибирає, що підходить, і бронює
    Це вписується ідеально:
        - каталог готелів → booking → створює Stay
        - одразу після створення:
            - генерується GuestAccessToken
            - на email/whatsapp летить лінк на той самий /guest/access/:token
        - Тобто гостьовий кабінет буде один і той же для:
            - бронювання через твій майбутній публічний сайт
            - бронювання, створені адмінами / редакторами

============================================================================
============================================================================
### ✅ ВИКОНАНО

#### Етап 1: Логіка та проєктування
- ✅ Визначено логіку отримання доступу гостями
- ✅ Визначено рівні доступу за статусами Stay (booked, occupied, completed, cancelled)
- ✅ Описано екрани та функції для кожного статусу
- ✅ Визначено, які дані показувати, а які приховувати

#### Етап 2: Бекенд (виконано в hotel-backend)
- ✅ Створено entity GuestAccessToken
- ✅ Додано зв'язок Stay ↔ GuestAccessToken
- ✅ Винесено типи в src/types/guest.ts
- ✅ Створено контролер guest.controller.ts
- ✅ Створено роути guest.routes.ts
- ✅ Реалізовано генерацію токенів
- ✅ Реалізовано валідацію токенів
- ✅ Реалізовано рівні доступу за статусами

### 📋 ЗАЛИШИЛОСЬ

#### Етап 3: Проєктування фронтенду
- ⏳ Створити структуру проекту (Vue 3, TypeScript, Vite)
- ⏳ Налаштувати роутинг з динамічними маршрутами
- ⏳ Проєктувати структуру сторінок для кожного статусу
- ⏳ Проєктувати компоненти (картки, форми, кнопки)

#### Етап 4: Реалізація фронтенду
- ⏳ Створити базову структуру проекту
- ⏳ Налаштувати API клієнт для Guest API
- ⏳ Реалізувати сторінку для статусу `booked`:
  - Відображення інформації про бронювання
  - Контакти готелю
  - Форма для коментарів/побажань
- ⏳ Реалізувати сторінку для статусу `occupied`:
  - Відображення Wi-Fi даних
  - Кнопка "Попросити прибирання"
  - Кнопка "Написати в ресепцію"
- ⏳ Реалізувати сторінку для статусу `completed`:
  - Подяка за проживання
  - Форма відгуку
- ⏳ Реалізувати сторінку для статусу `cancelled`:
  - Інформація про скасування
- ⏳ Налаштувати PWA (Service Worker, Manifest)
- ⏳ Додати адаптивний дизайн
- ⏳ Додати обробку помилок (невалідний токен, мережа)

#### Етап 5: Додаткові функції
- ⏳ POST `/guest/access/:token/requests/cleaning` — запит на прибирання
- ⏳ POST `/guest/access/:token/feedback` — залишити відгук
- ⏳ POST `/guest/access/:token/extend` — продовжити проживання
- ⏳ Офлайн доступ до базової інформації
- ⏳ Push-нотифікації

#### Тестування
- ⏳ Тестування на різних пристроях
- ⏳ Тестування офлайн режиму
- ⏳ Тестування PWA функцій
- ⏳ Тестування різних статусів Stay

### 🔮 МАЙБУТНІ ЗАВДАННЯ

#### 🤖 AI-консьєрж для гостей
**Інтеграція з AI-модулем:**
- AI-чат в гостьовому PWA
- Відповіді про заселення, Wi-Fi, правила, інфо про готель
- Погода, запит на прибирання
- Smart-chat: AI знає stayId, roomNumber, статус, дати, Wi-Fi, токен гостя
- Підтримка мов: uk/en/it/de

**UI компоненти:**
- `/components/ai/GuestAiChat.vue` — чат-інтерфейс для гостя
- `/components/ai/AiQuickActions.vue` — швидкі дії (прибирання, контакти)
- `/stores/ai.ts` — Pinia store для AI стану

**API інтеграція:**
- `POST /guest-ai/chat` — відправка повідомлень до AI (authorized by GuestToken)
- Автоматичне додавання контексту (stay, room, hotel)
- Відображення історії чату

**Функціональність:**
- Відповіді на питання про проживання
- Інформація про Wi-Fi, правила готелю
- Запит на прибирання через AI
- Контакти та інструкції
- Погода та місцеві події

#### Інтеграція з Booking Engine
- Коли буде готовий публічний сайт для бронювання
- Автоматична генерація токену після бронювання
- Відправка лінку на email/SMS

**Примітка:** Календар бронювання для публічного сайту буде реалізований в окремому проекті Booking Engine.

#### Покращення UX
- Персоналізація інтерфейсу
- Мультимовність
- Темна/світла тема
- Кастомізація під кожен готель

### 🤔 ЩО ПОТРІБНО ОБДУМАТИ

1. **Структура роутів:**
   - Як організувати маршрути? `/access/:token` або `/s/:token`?
   - Чи потрібні окремі роути для кожного статусу?

2. **Оновлення даних:**
   - Як часто оновлювати дані про проживання?
   - Чи потрібен real-time оновлення статусу?
   - Як обробляти зміну статусу під час перегляду?

3. **PWA функції:**
   - Які дані кешувати для офлайн доступу?
   - Чи потрібні push-нотифікації?
   - Як обробляти оновлення PWA?

4. **UX для різних статусів:**
   - Як краще відображати різні статуси?
   - Чи потрібні анімації при зміні статусу?
   - Як інформувати гостя про зміни?

### ⚠️ ПРОБЛЕМИ ТА ВИРІШЕННЯ

#### Відомі проблеми
- ⚠️ Потрібно визначитися зі структурою проекту
- ⚠️ Потрібно визначитися з UI/UX підходом
- ⚠️ Потрібно визначитися з PWA функціями

### 📝 ПРИМІТКИ

- Бекенд готовий, можна починати інтеграцію
- API endpoints готові та протестовані
- Типи даних визначені та узгоджені з бекендом
- Потрібно зосередитися на UX для мобільних пристроїв

---

**Останнє оновлення:** 2025-01-XX
**Версія:** 0.1.0 (в розробці)