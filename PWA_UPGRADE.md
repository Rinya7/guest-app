# PWA Upgrade - Production-Ready Hotel Guest App

## 📋 Огляд змін

Guest приложение було оновлено до production-ready PWA з повною підтримкою offline режиму та app-like UX.

## ✅ Реалізовані функції

### 1. Offline-First Стратегія

**IndexedDB для зберігання даних:**
- ✅ Використання IndexedDB замість LocalStorage (підтримка великих об'єктів, асинхронні операції)
- ✅ Автоматичне збереження успішних запитів API
- ✅ Відновлення даних з кешу при offline режимі
- ✅ Автоматична синхронізація при відновленні з'єднання
- ✅ Очищення застарілих даних (30 днів)

**Fallback сторінка:**
- ✅ `OfflinePage.vue` - показується при відсутності з'єднання та даних
- ✅ Індикатор використання кешованих даних в `GuestLayout`

**Online/Offline статус:**
- ✅ `OnlineStatus.vue` - компонент для відображення статусу підключення
- ✅ Автоматичне відстеження через `navigator.onLine` API
- ✅ Візуальні сповіщення при зміні статусу

### 2. Service Worker Покращення

**Версіонування кешу:**
- ✅ `cacheId: "hotel-guest-pwa-v1"` для коректних оновлень
- ✅ Версіонування кешів (`images-cache-v1`, `api-cache-v1`)
- ✅ Автоматичне очищення застарілих кешів (`cleanupOutdatedCaches: true`)

**Стратегії кешування:**
- ✅ Статика: CacheFirst (швидкий доступ)
- ✅ API: NetworkFirst з fallback на кеш (8s timeout)
- ✅ Збереження помилок мережі (status 0) для offline fallback

**Оновлення Service Worker:**
- ✅ `skipWaiting: true` + `clientsClaim: true` для миттєвих оновлень
- ✅ `UpdateNotification.vue` - сповіщення про доступність нової версії
- ✅ Автоматичне перезавантаження при зміні контролера

### 3. Installability & App UX

**iOS Safari підтримка:**
- ✅ `apple-mobile-web-app-capable` meta tag
- ✅ `apple-mobile-web-app-status-bar-style` для стилю статус-бару
- ✅ `apple-touch-icon` для іконки на домашньому екрані
- ✅ `viewport-fit=cover` для повноекранного режиму

**Android Chrome підтримка:**
- ✅ `usePWAInstall.ts` composable для обробки `beforeinstallprompt`
- ✅ Контрольований промпт установки (не агресивний)

**Manifest покращення:**
- ✅ `scope: "/"` для коректної роботи deep links
- ✅ Категорії для app stores (`travel`, `hospitality`)
- ✅ Правильні іконки з `maskable` purpose

### 4. Routing & Deep Links

**Оптимізація:**
- ✅ Lazy loading для route components (вже було, залишено)
- ✅ Fallback route для невідомих шляхів
- ✅ `scrollBehavior` для прокрутки вгору при навігації

**Deep Links:**
- ✅ Всі роути `/access/:token/...` працюють після установки
- ✅ `start_url: "/"` коректно обробляє токен-доступ
- ✅ Токен зберігається в URL (не в localStorage) для безпеки

### 5. Performance & UX

**Preload:**
- ✅ `preconnect` для API домену (швидше DNS lookup)

**UX покращення:**
- ✅ Індикатор кешованих даних (синій банер)
- ✅ Плавні переходи для статусів (Transition компоненти)
- ✅ Чіткі повідомлення про offline режим

### 6. Security

**Content Security Policy:**
- ✅ CSP meta tag з розумними обмеженнями
- ✅ Дозволено `unsafe-inline` для Vite dev server та PWA функцій
- ✅ Дозволено `blob:` для Service Worker
- ✅ Обмеження `connect-src` для API endpoints

**Безпека токена:**
- ✅ Токен ніколи не логується в console
- ✅ Токен правильно кодується через `encodeURIComponent`
- ✅ Токен зберігається тільки як ключ в IndexedDB (same-origin policy)
- ✅ Токен не передається в insecure контекстах

## 📁 Нові файли

```
guest/src/
├── utils/
│   ├── storage.ts          # IndexedDB утиліта
│   └── online.ts            # Online/offline статус
├── composables/
│   ├── usePWAInstall.ts     # PWA install handler
│   └── useServiceWorkerUpdate.ts  # SW update handler
├── components/
│   ├── OnlineStatus.vue    # Online/offline індикатор
│   └── UpdateNotification.vue  # SW update сповіщення
└── pages/
    └── OfflinePage.vue      # Offline fallback сторінка
```

## 🔄 Оновлені файли

- `src/stores/guest.ts` - offline-first стратегія з кешуванням
- `src/pages/AccessPage.vue` - обробка offline режиму
- `src/layouts/GuestLayout.vue` - індикатор кешованих даних
- `src/App.vue` - додано OnlineStatus та UpdateNotification
- `src/main.ts` - ініціалізація online статусу та cleanup
- `src/router/index.ts` - fallback route та scrollBehavior
- `vite.config.ts` - покращення Workbox конфігурації
- `index.html` - iOS meta tags, CSP, preconnect

## 🎯 Ключові рішення

### Чому IndexedDB замість LocalStorage?

1. **Розмір даних**: `GuestStayView` може містити багато полів (готель, кімната, гості, контакти)
2. **Асинхронність**: IndexedDB не блокує UI thread
3. **Структурованість**: Можна додати індекси для швидкого пошуку
4. **PWA стандарт**: IndexedDB рекомендований для PWA зберігання

### Чому NetworkFirst для API?

1. **Актуальність**: Завжди намагаємося отримати свіжі дані
2. **Offline fallback**: Якщо мережа недоступна - використовуємо кеш
3. **Timeout**: 8 секунд - баланс між очікуванням та швидкістю

### Чому CacheFirst для зображень?

1. **Швидкість**: Зображення рідко змінюються
2. **Пропускна здатність**: Економія трафіку для гостей
3. **Expiration**: 30 днів - достатньо для статичних ресурсів

## 🧪 Тестування

### Offline режим:
1. Відкрити додаток з токеном
2. Відключити інтернет
3. Перезавантажити сторінку
4. ✅ Має показати кешовані дані або OfflinePage

### Service Worker оновлення:
1. Зробити зміни в коді
2. Збілдити нову версію
3. Відкрити додаток
4. ✅ Має показати сповіщення про оновлення

### Installability:
1. Android Chrome: має з'явитися "Install" prompt
2. iOS Safari: "Add to Home Screen" в меню
3. ✅ Обидва мають працювати

## 📝 Примітки

- CSP може потребувати налаштування для конкретного API домену
- iOS Safari має обмеження на Service Worker (працює тільки в standalone режимі)
- IndexedDB очищається автоматично через 30 днів (налаштовується в `cleanupOldData`)

## 🚀 Деплой

**⚠️ ВАЖЛИВО:** Деплой виконується на сервері, не локально!

### Процес деплою:

**1. Локально (на вашому комп'ютері):**
```bash
cd guest
git add .
git commit -m "PWA upgrade: offline-first, installability, SW updates"
git push
```

**2. На сервері (після пушу):**
```bash
# Підключитися до сервера
ssh root@46.224.81.114

# Перейти в директорію guest
cd /opt/hotel/guest

# Оновити код з GitHub
git pull

# Перевірити наявність .env.production (створюється один раз)
cat .env.production
# Має містити: VITE_API_URL=https://api.hotel-lotse.app

# ПЕРЕСОБРАТИ КОНТЕЙНЕР (важливо після змін у коді)
docker compose build --no-cache

# Перезапустити контейнер
docker compose up -d

# Перевірити логи
docker logs hotel-guest
```

**3. Перевірка після деплою:**

1. **Перевірка контейнера:**
   ```bash
   docker ps | grep guest
   docker logs hotel-guest
   ```

2. **Перевірка PWA функцій в браузері:**
   - Відкрити: https://guest.hotel-lotse.app
   - DevTools → Application → Service Workers (перевірити статус SW)
   - DevTools → Application → Storage → IndexedDB (перевірити кешовані дані)
   - DevTools → Network → Offline (тест offline режиму)
   - Перевірити manifest: https://guest.hotel-lotse.app/manifest.webmanifest

**Примітка:** `npm run build` виконується автоматично всередині Dockerfile під час `docker compose build`. Не потрібно запускати його локально перед пушем.

---

**Версія:** 1.0.0  
**Дата:** 2024  
**Статус:** ✅ Production-ready PWA

