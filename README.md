# Guest App - PWA для гостей готелів

## 📖 Опис

Progressive Web App (PWA) для гостей готелів. Дозволяє гостям отримувати доступ до інформації про своє проживання через унікальний токен доступу, без необхідності реєстрації або авторизації.

## 🚀 Швидкий старт

### Встановлення залежностей

```bash
npm install
```

### Запуск в режимі розробки

```bash
npm run dev
```

Додаток буде доступний на http://localhost:5174

### Збірка для production

```bash
npm run build
```

## 🏗️ Структура проекту

```
guest-app/
├── src/
│   ├── api/          # API клієнти для взаємодії з backend
│   │   ├── guest.ts  # Функції для роботи з Guest API
│   │   └── http.ts   # Axios інстанс з інтерцепторами
│   ├── components/   # Vue компоненти
│   │   ├── Loader.vue        # Компонент завантаження
│   │   ├── ErrorMessage.vue   # Компонент помилок
│   │   └── InfoBlock.vue     # Універсальний інформаційний блок
│   ├── pages/        # Сторінки додатку
│   │   ├── AccessPage.vue      # Головна сторінка доступу (завантаження та перенаправлення)
│   │   ├── StayInfoPage.vue    # Інформація про проживання
│   │   ├── WifiPage.vue        # Wi-Fi інформація
│   │   ├── ContactPage.vue     # Контакти готелю
│   │   ├── CompletedPage.vue   # Сторінка для завершеного проживання
│   │   └── CancelledPage.vue   # Сторінка для скасованого бронювання
│   ├── layouts/      # Layout компоненти
│   │   └── GuestLayout.vue     # Основний layout з header та footer
│   ├── stores/       # Pinia stores
│   │   └── guest.ts  # Store для даних гостя
│   ├── router/       # Vue Router конфігурація
│   │   └── index.ts  # Роути додатку
│   ├── types/        # TypeScript типи
│   │   └── guest.ts  # Типи для Guest API
│   ├── styles/       # Глобальні стилі
│   │   └── base.css  # TailwindCSS базові стилі
│   ├── App.vue       # Головний компонент
│   └── main.ts       # Точка входу
├── public/           # Статичні файли
└── package.json
```

## 📡 API Integration

Додаток інтегрується з `hotel-backend` через REST API.

**Base URL:** налаштовується через змінну середовища `VITE_API_URL` (за замовчуванням: `http://localhost:3000`)

## 🔗 Доступ по токену

Гість отримує доступ через унікальний URL:
```
https://guest.example.com/access/{token}
```

Токен генерується адміністратором через API endpoint:
```
POST /guest/stays/:stayId/link
```

## 🛠️ Технології

- **Vue 3** (Composition API)
- **TypeScript** (strict mode, без `any`)
- **Vite** (build tool)
- **Vue Router** (routing)
- **Pinia** (state management)
- **Axios** (HTTP client)
- **TailwindCSS** (стилізація)
- **Vite PWA** (PWA функціональність з Workbox)

## 📝 Статуси проживання та доступ

Додаток показує різний контент залежно від статусу проживання:

- **booked** - Бронювання підтверджено (до заїзду)
  - Показує: дати, дані готелю, контакти, політику заїзду/виїзду
  - Wi-Fi недоступний
  
- **occupied** - Гість заїхав
  - Показує: повний доступ, Wi-Fi інформацію, номер, інструкції
  
- **completed** - Проживання завершено
  - Read-only історія проживання, подяка
  
- **cancelled** - Бронювання скасовано
  - Інформація про скасування, контакти для зв'язку

## 🔧 Налаштування

### Environment Variables

```env
VITE_API_URL=http://localhost:3000
```

## 🗺️ Роути

- `/` - Головна сторінка (AccessPage)
- `/access/:token` - Доступ по токену (AccessPage - завантажує дані та перенаправляє)
- `/access/:token/stay` - Інформація про проживання (StayInfoPage)
- `/access/:token/wifi` - Wi-Fi інформація (WifiPage)
- `/access/:token/contact` - Контакти готелю (ContactPage)
- `/access/:token/completed` - Сторінка для завершеного проживання (CompletedPage)
- `/access/:token/cancelled` - Сторінка для скасованого бронювання (CancelledPage)

## 🔄 Flow гостя

1. Гість отримує унікальний URL з токеном: `/access/{token}`
2. `AccessPage.vue` завантажує дані через `guestStore.loadStayData(token)`
3. Залежно від статусу (`stayStatus`) відбувається автоматичне перенаправлення:
   - `booked` або `occupied` → `/access/:token/stay`
   - `completed` → `/access/:token/completed`
   - `cancelled` → `/access/:token/cancelled`
4. Гість може переходити між розділами через навігацію

---

**Версія:** 0.0.1  
**Статус:** В розробці (перша версія готова)

